BONUS
arguments to be passed: CPU or MEM, threshold value, polling_interval value, Moving_window size.




$ sudo python bonus.py CPU --threshold=0.2 --polling_interval 2 --moving_window 2

cat alerts.csv

cat mov_avgs.csv